var fs = require('fs');
var jsons = require('../');

var read = fs.createReadStream(__dirname + '/randwords.json');

read
  .pipe(jsons.parse())
  .pipe(jsons.stringify())
  .pipe(jsons.parse())
  .pipe(process.stdout);
